# Enable mouse control
set -g mouse on

# Increase scroll history
set-option -g history-limit 10000

# Use vim keybindings in scroll mode
set-window-option -g mode-keys vi

# Set vi-style copy and paste
bind-key -T copy-mode-vi v send-keys -X begin-selection
bind-key -T copy-mode-vi y send-keys -X copy-selection

# ==========================
# TPM & Plugins Configuration
# ==========================

# List of plugins
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-cpu'

# Configure the status bar right side to use the CPU, RAM, and GPU metrics
set -g status-right-length 100
set -g status-right "CPU: #{cpu_percentage} | RAM: #{ram_percentage} | GPU: #{gpu_percentage} | VRAM: #{gram_percentage} | %Y-%m-%d %H:%M "

# Automatic TPM installation (clones TPM and installs plugins if TPM is not found)
if "test ! -d ~/.tmux/plugins/tpm" \
   "run 'git clone https://github.com/tmux-plugins/tpm ~/.tmux/plugins/tpm && ~/.tmux/plugins/tpm/bin/install_plugins'"

# Initialize TMUX plugin manager (keep this line at the very bottom of tmux.conf)
run '~/.tmux/plugins/tpm/tpm'