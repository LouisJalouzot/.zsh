# Installation

```bash
git clone https://github.com/LouisJalouzot/.zsh.git ~/.zsh
ln -s ~/.zsh/.zshrc ~/.zshrc
source ~/.zshrc
```

# Update Plugins

```bash
rm -rf ~/.zsh/plugins
source ~/.zshrc
```