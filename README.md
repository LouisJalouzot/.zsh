# Installation

```bash
git clone https://github.com/LouisJalouzot/.zsh.git ~/.zsh
ln -s ~/.zsh/.zshrc ~/.zshrc
ln -s ~/.zsh/.p10k.zsh ~/.p10k.zsh
source ~/.zshrc
```

# Update Plugins

```bash
rm -rf ~/.zsh/plugins
source ~/.zshrc
```